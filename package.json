{
  "name": "reinforcement-learning",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "esbuild src/main.ts --bundle --format=esm --sourcemap --outfile=dist/main.js --watch --servedir=.",
    "build": "esbuild src/main.ts --bundle --format=esm --minify --outfile=dist/main.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.24.2",
    "eslint": "^9.0.0",
    "eslint-plugin-simple-import-sort": "^12.1.1",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.54.0"
  }
}
